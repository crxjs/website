---
title: 快速开始
description: vitest-environment-web-ext是一个专为浏览器扩展（Browser Extension）设计的Vitest测试环境，用于实现浏览器扩展的端到端（E2E）测试。该项目基于Playwright构建，提供对Chrome扩展的完整测试支持。
icon: 'lucide:rocket'
---

## 安装依赖

::pm-install{name="vitest-environment-web-ext" save-dev}
::

## 配置Vitest

:::code-group

```ts [vitest.config.ts]
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'web-ext',
    environmentOptions: {
      'web-ext': {
        path: './dist',
      },
    },
  },
})
```

:::

## 更新`package.json`
```json{3} [package.json]
{
  "scripts": {
    "test": "vitest"
  }
}
```

## 编写测试

:::code-group

```ts [test/extension.test.ts]
import { expect, test } from 'vitest'

test('弹出窗口应正确显示', async () => {
  const popupPage = await browser.getPopupPage()

  const title = await popupPage.title()
  expect(title).toBeTruthy()
})
```

:::

## 运行测试

::pm-run{script="test"}
::
